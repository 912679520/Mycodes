#include<iostream>
#include<math.h>
#include<vector>
using namespace std;


/*
class Solution {
public:
    vector<int> printNumbers(int n) 
    {
        const int num = pow(10, n);
        vector<int> arr(num - 1);
        for (int i = 0; i < num - 1; i++)
        {
            arr[i] = i + 1;
        }
        return arr;
    }
};

int main()
{
    int n;
    cin >> n;
    Solution a;
    vector<int> arr = a.printNumbers(n);
    for (int i = 0; i < arr.size();i++)
    {
        cout << arr[i] << " ";
    }
    cout << endl;

    return 0;
}*/